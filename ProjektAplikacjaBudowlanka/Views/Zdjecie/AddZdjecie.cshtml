@using ProjektAplikacjaBudowlanka.Models.DTO.Zdjecie
@model ZdjecieAddDTO;

<h2>Dodaj zdjęcie</h2>

@* @using (Html.BeginForm("AddZdjecie", "Zdjecie", FormMethod.Post, new { enctype = "multipart/form-data" }))
{

    <div class="form-group">
        @Html.LabelFor(model => model.Zdjecie, htmlAttributes: new { @class = "control-label" })
        @Html.TextBoxFor(model => model.Zdjecie, new { type = "file", @class = "form-control-file", accept = "image/*", required = "required" })
        @Html.ValidationMessageFor(model => model.Zdjecie, "", new { @class = "text-danger" })
    </div>

    <button type="submit" class="btn btn-primary">Dodaj zdjęcie</button>
} *@
<form enctype="multipart/form-data" method="post">
    <dl>
        <dt>
            <label asp-for="FileUpload.FormFile"></label>
        </dt>
        <dd>
            <input asp-for="FileUpload.FormFile" type="file">
            <span asp-validation-for="FileUpload.FormFile"></span>
        </dd>
    </dl>
    <input asp-page-handler="Upload" class="btn" type="submit" value="Upload" />
</form>
